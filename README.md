## Installing

To install all dependencies, run the command

`npm i`

## Run the code

To run the code in dev mode, use the command at the root level

`npx nodemon ./index.ts`

## Testing

To run tests, run the command

`npm run test`
